Please place the 'DepotDownloader.exe' file (and any associated files if using the non-standalone version) in this directory.

The application will look for:
resources/DepotDownloader/DepotDownloader.exe

If you are using the .NET Core version, ensure the dotnet runtime is installed on the machine.
